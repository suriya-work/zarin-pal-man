import React from 'react';
import Headers from './Headers';
import Banner from './Banner';

const Landing = () => {
    return (
        <div>
            <Headers />
            <Banner />
        </div>
    );
};

export default Landing;